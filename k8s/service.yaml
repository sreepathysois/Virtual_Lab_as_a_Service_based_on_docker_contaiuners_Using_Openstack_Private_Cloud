apiVersion: v1
kind: Service
metadata:
  name: vlab-jupyter-svc
spec:
  type: NodePort
  selector:
    app: vlab-jupyter
  ports:
    - protocol: TCP
      port: 8888
      targetPort: 8888
      nodePort: 30888   # Access via http://<node-ip>:30888
